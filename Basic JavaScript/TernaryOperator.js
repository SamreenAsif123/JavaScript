//Ternary Operator
let israining= true
let weatherMessage =israining? 'Bring an Umbrella': 'No need for an Umbrella'
console.log(weatherMessage);

let number1 = 5 
    number2 = 6
let largerNumber= (number1>number2)? number1 : number2;
console.log ('The largest nunmber is :',largerNumber);

let marks = 50;
let passingMarks= marks> 35? 'Passed':'Fail'
console.log(passingMarks);

let number = 8;
let sign = (number >= 9) ? "Positive" : "Negative";
console.log(sign);

let num1 = -6;
let absValue = (num1 >= 0) ? num1 : -num1;
console.log(absValue);

let score= 80
let grade= (score>=90)? 'A+':
           (score>=80)? 'A+':
           (score>=70)? 'A':
           (score>=60)? 'B+':
           (score>=35)? 'C': 'F'
console.log (grade);


let score1= 32
let grade2= (score1>=90)? 'A+':
           (score1>=80)? 'A+':
           (score1>=70)? 'A':
           (score1>=60)? 'B+':
           (score1>=35)? 'C': 'F'
console.log (grade2);









